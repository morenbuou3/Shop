---
applications:
-
  # application name
  name: Shop

  # random route, avoid can not request a route
  random-route: true

  # refer spec buildpack
  buildpack: java_buildpack

  # path
  path: target/shop-1.0.BUILD-SNAPSHOT.jar